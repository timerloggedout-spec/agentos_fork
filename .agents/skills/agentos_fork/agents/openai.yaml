interface:
  display_name: "Agentos Fork"
  short_description: "Repo-specific patterns and workflows for agentos_fork"
  default_prompt: "Use the agentos_fork repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true